root (Tk)
L-- container (Frame)
    +-- b_frame (Frame)
    ¦   +-- open_folder_button (Button)
    ¦   +-- find_replace_button (Button)
    ¦   +-- save_button (Button)
    ¦   +-- cancel_button (Button)
    ¦   +-- delete_button (Button)
    ¦   +-- new_folder_button (Button)
    ¦   +-- auto_caption_button (Button)
    ¦   +-- caption_all_button (Button)
    ¦   L-- llm_settings_button (Button)
    L-- main_frame (Frame)
        +-- img_frame (Frame)
        ¦   +-- img_ctrl_frame (Frame)
        ¦   ¦   +-- prev_button (Button)
        ¦   ¦   +-- next_button (Button)
        ¦   ¦   +-- index_label (Label)
        ¦   ¦   +-- dir_entry (Combobox)
        ¦   ¦   L-- file_entry (Entry)
        ¦   +-- image_label (Label)
        ¦   L-- text_frame (Frame)
        ¦       +-- caption_frame (Frame)
        ¦       ¦   +-- text_area (Text)
        ¦       ¦   L-- text_area_scrollbar (yscroll for text_area)
        ¦       L-- right_notebook (ttk.Notebook)  [EXIF tab active on startup]
        ¦           +-- exif_frame (Frame, tab "EXIF")
        ¦           ¦   +-- exif_text_area (Text, read-only)
        ¦           ¦   L-- exif_text_scrollbar (yscroll for exif_text_area)
        ¦           L-- trans_frame (Frame, tab "Translate")
        ¦               +-- trans_button (Button)
        ¦               +-- text_lang (Text)
        ¦               L-- trans_text_area (Text)
        L-- nav_frame (Frame)
            +-- filter_frame (Frame)
            ¦   +-- filter_label (Label)
            ¦   +-- filter_entry (Entry)
            ¦   +-- clear_filter_button (Button)
            ¦   L-- show_empty_checkbox (Checkbutton)
            +-- mode_frame (Frame)
            ¦   +-- list_mode_btn (Button)
            ¦   +-- thumb_mode_btn (Button)
            ¦   +-- thumb_progress_bar (Progressbar)  [also reused by "Caption all" batch progress]
            ¦   L-- thumb_progress_label (Label)  [batch shows Captioning progress]
            +-- file_list (ttk.Treeview: columns path, len)
            +-- scrollbar (Scrollbar, yscroll for file_list)
            L-- thumb_view.frame (Frame)  [created by ThumbnailView; shown only in thumbs mode]
                +-- thumb_view._canvas (Canvas)  [viewport + scrollregion; takes focus for keyboard nav]
                +-- thumb_view._scrollbar (Scrollbar)
                L-- [Virtualized dynamic thumb cells mounted into Canvas via create_window]
                    L-- Each mounted cell (Frame)
                        +-- placeholder (Canvas) OR img_lbl (Label with PhotoImage)
                        L-- label_frame (Frame)
                            +-- dot (Label "●", green/red by has_caption)
                            L-- name_lbl (Label, shortened filename)

LLM Settings dialog (auto_caption._SettingsDialog, Toplevel)  [modal; opened by llm_settings_button]
L-- body (Frame)
    +-- base_url_entry (Entry, "OpenAI-compatible base URL")
    +-- api_key_entry (Entry, "API key", masked)
    +-- model_row (Frame, "Model name")
    ¦   +-- model_combo (ttk.Combobox, editable; blank = auto-detect)
    ¦   L-- refresh_models_button (Button "Refresh", -> list_models)
    +-- model_hint_label (Label "(blank = auto-detect)")
    +-- max_tokens_entry (Entry, "Max tokens")
    +-- temperature_entry (Entry, "Temperature")
    +-- timeout_entry (Entry, "Timeout (s)")
    +-- image_format_combo (ttk.Combobox: auto/original/png/jpeg)
    +-- system_prompt_text (Text, "System prompt")
    L-- user_prompt_text (Text, "User prompt")
L-- btns (Frame)
    +-- test_button (Button "Test connection", -> list_models, left)
    +-- cancel_button (Button "Cancel", right)
    L-- save_button (Button "Save", -> LLMSettings.save to auto_caption_settings.ini, right)